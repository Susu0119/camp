<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
	PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
        
<mapper namespace="com.m4gi.mapper.CampgroundMapper">
	
	<!-- 캠핑장 목록 조회 ""미완성""-->
	<select id="" resultType="com.m4gi.dto.CampgroundDTO">
		SELECT campground_id AS campgroundId
		FROM campgrounds
		WHERE 
			campground_name LIKE CONCAT('%', #{campgroundName}, '%')
        AND addr_sigungu IN
            <foreach item="sigungu" collection="addrSiGunguList" open="(" separator="," close=")">
                #{sigungu}
            </foreach>
	</select>
	<!-- 캠핑장 목록 조회 -->
	
	
	
	
</mapper>